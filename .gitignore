.DS_Store
node_modules
build
dist
package
.env
.env.*
!.env.example
!.env.*.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

.vscode
.op

codegenCache.json

cache

**/target

# Bun
.bun

# Playwright
test-results/
playwright-report/

# Defensive ignores for stale Terraform/OpenTofu state in existing clones. The configuration is gone,
# but state and variable files can contain credentials and must never become visible to Git.
.terraform/
*.tfstate
*.tfstate.*
terraform.tfvars

# Yarn (not used — this is a Bun workspace)
.pnp.*
.yarn/
yarn.lock

# Claude Code local runtime state (settings.local.json is already ignored globally)
.claude/scheduled_tasks.lock
.playwright-mcp/

# NSIS hook-guard harness build output (packages/presto/src-tauri/nsis/harness.test.nsi)
hooks-harness-setup.exe
