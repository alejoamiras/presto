{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Presto Verified Sites Registry",
  "description": "Curated list of origins recognized by the Presto maintainers. NOT a security guarantee — see VERIFIED_SITES.md.",
  "type": "object",
  "required": ["schemaVersion", "entries"],
  "additionalProperties": false,
  "properties": {
    "$schema": { "type": "string" },
    "schemaVersion": {
      "type": "integer",
      "const": 1,
      "description": "Schema version. Bump on breaking changes."
    },
    "entries": {
      "type": "array",
      "items": { "$ref": "#/$defs/entry" }
    }
  },
  "$defs": {
    "entry": {
      "type": "object",
      "required": ["origins", "displayName", "curatedBy", "addedAt"],
      "additionalProperties": false,
      "properties": {
        "origins": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "description": "Canonical origin: scheme://host[:port], no trailing slash, no path. ASCII only (use Punycode A-labels for IDN). For browser extensions, ONLY chrome-extension:// is reliably curatable; Firefox install UUIDs and Safari schemes are per-user and excluded.",
            "pattern": "^(https?|wss?|chrome-extension)://[A-Za-z0-9.-]+(:[0-9]+)?$"
          }
        },
        "displayName": {
          "type": "string",
          "minLength": 1,
          "maxLength": 64
        },
        "description": {
          "type": "string",
          "maxLength": 200,
          "description": "Internal note — NOT shown in the authorization popup. Used in docs / future Settings UI."
        },
        "curatedBy": {
          "type": "string",
          "description": "GitHub username of the curator who reviewed/added the entry."
        },
        "addedAt": {
          "type": "string",
          "format": "date",
          "description": "ISO date for audit trail."
        }
      }
    }
  }
}
