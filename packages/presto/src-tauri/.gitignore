/target
/binaries
AZTEC_VERSION
# F-012: generated per-page frontend bundles (built from frontend-src/ by `bun run frontend:build`).
# Never committed — CI rebuilds them; build.rs fails if they are missing or stale vs the sources.
/frontend/assets
# F-012: tauri-build autogenerates allow-/deny- permission defs from build.rs AppManifest.commands()
# on every build ("DO NOT EDIT" headers) — pure output derived from the COMMANDS list, so not committed.
/permissions/autogenerated
