{
  "name": "@alejoamiras/presto-release-feed",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "test": "bun test",
    "typecheck": "wrangler types --check && tsc --noEmit",
    "types": "wrangler types"
  }
}
