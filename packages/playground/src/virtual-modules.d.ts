/** Provided by `noirFixturePlugin` in vite.config.ts: the committed `square` fixture, base64. */
declare module "virtual:noir-fixture" {
  const fixture: import("./noir").NoirFixtureModule;
  export default fixture;
}
