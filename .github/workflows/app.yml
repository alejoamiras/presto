name: App

on:
  workflow_dispatch:
  pull_request:
    branches: [main, security-hardening]

# Cancel a superseded in-flight run when a new commit is pushed to the same PR.
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  changes:
    name: Detect changes
    runs-on: ubuntu-latest
    timeout-minutes: 2
    outputs:
      relevant: ${{ steps.override.outputs.relevant || steps.filter.outputs.relevant }}
      lna_relevant: ${{ steps.override.outputs.relevant || steps.filter.outputs.lna_relevant }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: dorny/paths-filter@ceb8a2b8f2d89434be7ff52d3de7ec3738c5cc9d # v4.0.3
        id: filter
        with:
          filters: |
            relevant:
              - 'packages/playground/**'
              - 'packages/sdk/**'
              - 'packages/sdk-core/**'
              - 'tsconfig.json'
              - 'biome.json'
              - 'package.json'
              - 'bun.lock'
              - 'bunfig.toml'
              - '.bun-version'
              - 'rust-toolchain.toml'
              - '.github/workflows/app.yml'
              - '.github/workflows/_e2e-app.yml'
              - '.github/actions/setup-aztec/**'
              - '.github/actions/setup-presto/**'
              - '.github/actions/start-services/**'
            # Fail closed for the real Chromium Local Network Access boundary. This is independent
            # from `relevant`: a landing-only change runs this focused browser job without waking the
            # expensive Aztec node/proving matrix below.
            lna_relevant:
              - 'packages/sdk/**'
              - 'packages/sdk-core/**'
              - 'packages/playground/**'
              - 'packages/landing/**'
              - 'tsconfig*.json'
              - 'biome.json'
              - 'package.json'
              - 'bun.lock'
              - 'bunfig.toml'
              - '.bun-version'
              - '.github/actions/playwright-cache/**'
              - '.github/workflows/app.yml'
      - name: Override for manual dispatch
        if: github.event_name == 'workflow_dispatch'
        id: override
        run: echo "relevant=true" >> "$GITHUB_OUTPUT"

  lint:
    name: Lint
    needs: changes
    if: needs.changes.outputs.relevant == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version-file: .bun-version
      - uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('bun.lock') }}
          restore-keys: ${{ runner.os }}-bun-
      - run: bun install --frozen-lockfile
      - run: bun run lint
      # Typecheck all three playground graphs: src (browser types), tests (bun types), and
      # e2e/config (node types) — `vite build` and `bun test` check none of them. Catches
      # @aztec API breaks at compile time, e.g. the rc.2 AztecAddress.fromBigInt removal.
      - run: bun run --cwd packages/playground typecheck
      # Typecheck the FPC deploy/fund scripts too (same escape class, own tsconfig).
      - run: bun run --cwd packages/playground typecheck:scripts

  unit-tests:
    name: Unit Tests
    needs: changes
    if: needs.changes.outputs.relevant == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version-file: .bun-version
      - uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('bun.lock') }}
          restore-keys: ${{ runner.os }}-bun-
      - run: bun install --frozen-lockfile
      - run: bun run --cwd packages/playground test:unit

  mocked-e2e:
    name: Mocked E2E
    needs: changes
    if: needs.changes.outputs.relevant == 'true'
    runs-on: ubuntu-latest
    # Headroom for a cold Playwright fill (~30 min) on a cache miss; ~1-2 min on
    # a cache hit, so this ceiling is a safety net normal runs never approach.
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version-file: .bun-version
      - uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('bun.lock') }}
          restore-keys: ${{ runner.os }}-bun-
      - run: bun install --frozen-lockfile
      - uses: ./.github/actions/playwright-cache
      - run: bun run --cwd packages/playground test:e2e

  production-smoke:
    name: Production Build Smoke
    needs: changes
    if: needs.changes.outputs.relevant == 'true'
    runs-on: ubuntu-latest
    # Headroom for a cold Playwright fill (~30 min) on a cache miss; ~1-2 min on
    # a cache hit, so this ceiling is a safety net normal runs never approach.
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version-file: .bun-version
      - uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('bun.lock') }}
          restore-keys: ${{ runner.os }}-bun-
      - run: bun install --frozen-lockfile
      - uses: ./.github/actions/playwright-cache
      - run: bun run --cwd packages/playground test:e2e:production-smoke

  local-network-e2e:
    name: Local Network E2E
    needs: changes
    if: needs.changes.outputs.relevant == 'true'
    uses: ./.github/workflows/_e2e-app.yml
    with:
      test_script: "test:e2e:local-network"
      build_presto: true

  chromium-lna-e2e:
    name: Chromium LNA E2E
    needs: changes
    if: needs.changes.outputs.lna_relevant == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version-file: .bun-version
      - uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('bun.lock') }}
          restore-keys: ${{ runner.os }}-bun-
      - run: bun install --frozen-lockfile
      - uses: ./.github/actions/playwright-cache
        with:
          package-dir: packages/playground
      - run: bun run --cwd packages/playground test:e2e:lna

  app-status:
    name: App Status
    if: always()
    needs: [changes, lint, unit-tests, mocked-e2e, production-smoke, local-network-e2e, chromium-lna-e2e]
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Check results
        run: |
          results="${{ needs.changes.result }} ${{ needs.lint.result }} ${{ needs.unit-tests.result }} ${{ needs.mocked-e2e.result }} ${{ needs.production-smoke.result }} ${{ needs.local-network-e2e.result }} ${{ needs.chromium-lna-e2e.result }}"
          for r in $results; do
            if [ "$r" = "failure" ] || [ "$r" = "cancelled" ]; then
              echo "Job failed or was cancelled: $r"
              exit 1
            fi
          done
          echo "All jobs passed or were skipped"
