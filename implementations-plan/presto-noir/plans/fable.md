# Fable planning leg (Fable 5.1, Plan subagent) — verbatim return

Summary of positions (full text preserved from the subagent's final message):

- Directory names `packages/sdk-core` / `packages/sdk-noir` (npm names unchanged) to resolve the Rust-crate "core" overload.
- Wire contract: JSON + base64, unknown keys tolerated, response `{proof, public_inputs, vk?}` + `x-prove-duration-ms`; new codes `invalid_request`, `invalid_verifier_target`.
- `/health`: `schemes` in the detailed body only; `api_version` stays 1; no bb mapping field (documentation + required `bbVersion` adapter option).
- Rust: split `prove()` into `admit` (auth → inflight → Content-Length → body → header → status) and `acquire_prover` (resolve → download → threads → lease → permit) with RAII contexts, no behaviour change (existing tests pass unedited); extract `run_bb` from `prove_with_timeout`; new `ultra_honk.rs` handler with validation between the two stages; `bb::prove_ultra_honk` never touches the header prepend; `core/tests/ultra_honk_real_bb.rs` `#[ignore]`d real-bb integration test.
- TS: `PrestoClient` in core hoisting probe/generation/HTTPS-downgrade/F14 fallback table/phases from `PrestoProver`; `prove({path, contentType, body}) → ProveOutcome`; `PrestoProver` shrinks to serialize/decode; `PrestoUltraHonkBackend(bytecode, api | factory, {bbVersion (required), presto?, onPhase?, verificationKey?})`; `verifyProof` via WASM `UltraHonkVerifierBackend` with the cached VK; peer `>=5.2.0 <6`.
- Non-obvious: bb.js `Barretenberg.new()` in Node/Bun prefers the native backend — byte-identity tests must pin `BackendType.WasmWorker` (verified by main); bb.js default target when `verifierTarget` omitted must be resolved explicitly; cache `~/.bb-crs` in CI; `npm pack` does not rewrite `workspace:*`.
- Phases P1–P19 in five arcs: route → publish tooling → core → noir → playground/docs; real-bb job separate from the bb-free `smoke` job; composite action `start-headless-presto` shared by the Rust job and the sdk-noir live e2e; WebDriver spec with sidecar `bb verify` fallback if cross-arch bytes differ.
- Security: strict base64, decoded caps 16/32 MiB + 64 KiB, gzip magic, typed enum, capped inflate dry-run (256 MiB) as cheap bomb hardening, no new `@aztec` dep (committed witness, no noir_js), OIDC only in `_publish-npm.yml`, mining: log `scheme`/origin/`elapsed_ms` at info for the metering follow-up; no rate limits.
- Asks: commit yacana W's source or a stand-in? dir names ok? capped inflate dry-run? yacana origin? version scheme (manifest semver)? playground direct `@aztec/bb.js@5.2.0`?
- Forks: (a) two-phase prelude + `run_bb`; (b) optional VK / `--write_vk` / client cache / `verificationKey` seed; (c)(d) JSON+base64; (e) api_version 1 + `schemes` detailed-only, 404 remembered per generation; (f) documentation only; (g) peer `>=5.2.0 <6`, `Barretenberg | factory`; (h) commit source+artifact+witness+vk+expected proof+manifest, `--verify` in CI, `--regenerate` locally; (i) parameterize + one `_publish-npm.yml`, manifest semver for core/noir; (j) route → tooling → core → noir → playground; (k) new presto.yml job with `#[ignore]`d Rust test; WASM proof at test time with `BackendType.WasmWorker` vs committed native-generated expected proof.
